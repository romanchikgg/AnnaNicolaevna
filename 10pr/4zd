total = 0

while True:
    price = input("Введите цену или 0 для выхода: ")
    if price == "0":
        break
    price = int(price)
    if price < 0:
        print("Ошибка")
        continue
    total += price

if total > 2000:
    total = total * 0.7

print("К оплате:", total)
